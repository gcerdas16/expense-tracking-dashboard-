# URLs de Google Sheets CSV públicas
EXPENSES_CSV_URL=https://docs.google.com/spreadsheets/d/e/YOUR_SHEET_ID/pub?gid=0&single=true&output=csv
INCOMES_CSV_URL=https://docs.google.com/spreadsheets/d/e/YOUR_SHEET_ID/pub?gid=YOUR_GID&single=true&output=csv

# Hash de contraseña generado con bcrypt
# Genera uno con: node generar-hash-correcto.js
PASSWORD_HASH=your_bcrypt_hash_here

# Secreto de sesión (mínimo 32 caracteres)
# Genera uno con: node -p "require('crypto').randomBytes(32).toString('base64')"
SESSION_SECRET=your_session_secret_here_minimum_32_characters
